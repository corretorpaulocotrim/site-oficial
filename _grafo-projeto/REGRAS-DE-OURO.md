---
tipo: regra
---
# Regras de Ouro
1. Não marcar tarefa feita sem verificar (`grep`/render/`node --check`).
2. Não inventar arquivo/valor/imóvel/telefone/API.
3. Sempre `node --check` no JS inline (split em `</script>`) antes de publicar.
4. Ler só o que precisa — usar [[00-MAPA]] + grep.
5. Antes de reconstruir HTML, diffar contra último commit bom.
